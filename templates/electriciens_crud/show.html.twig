{% extends 'base.html.twig' %}

{% block title %}Show Electricien{% endblock %}

{% block body %}
    <h1>L'electricien {{bricoleurs_electricien.Name}}</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ bricoleurs_electricien.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ bricoleurs_electricien.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_electricien.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_electricien.Phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_electricien.Job }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_electriciens_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_electriciens_crud_edit', {'id': bricoleurs_electricien.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>
    {{ include('electriciens_crud/_delete_form.html.twig') }}
{% endblock %}
