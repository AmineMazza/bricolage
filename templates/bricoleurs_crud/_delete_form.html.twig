<form method="post" action="{{ path('app_bricoleurs_crud_delete', {'id': bricoleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bricoleur.id) }}">
    <button class="btn">Delete</button>
</form>
