{% extends 'base.html.twig' %}

{% block title %}Bricoleurs{% endblock %}

{% block body %}
    <h1>Bricoleurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bricoleur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bricoleur.Name }}</td>
            </tr>
            <tr>
                <th>Last_Name</th>
                <td>{{ bricoleur.LastName }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ bricoleur.Tel }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleur.Job }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bricoleurs_crud_index') }}">back to list</a>

    <a href="{{ path('app_bricoleurs_crud_edit', {'id': bricoleur.id}) }}">edit</a>

    {{ include('bricoleurs_crud/_delete_form.html.twig') }}
{% endblock %}
