{% extends 'base.html.twig' %}

{% block title %}Demenageurs{% endblock %}

{% block body %}

            <div class="row">
					<div class="col-md-6" style="margin:40px">
						<!-- Minuisiers list -->
						<div class="card" style="width:1000px">
							<div class="card-header header-elements-inline">
                                <h5 class="card-title" style="color:blue">Les Demenageurs</h5>
								<div class="header-elements">
                                    <div class="list-icons">
				                		<a class="icon-plus-circle2" href="{{ path('app_demenageurs_crud_new') }}" ></a>
		                		        <a class="list-icons-item" style="color:blue" data-action="collapse"></a>
		                		        <a class="list-icons-item" style="color:blue" data-action="reload"></a>
		                		        <a class="list-icons-item" style="color:blue" data-action="remove"></a>
		                	        </div>
			                	</div>
							</div>
				<table class="table datatable-basic">
                    <thead>
                        <tr>
                            {# <th>Id</th> #}
                            <th>Name</th>
                            <th>Last_name</th>
                            <th>Phone</th>
                            <th>Job</th>
                            <th class="text-center">actions</th>
                        </tr>
                    </thead>
					<tbody>
					 {% for bricoleurs_demenageur in bricoleurs_demenageurs %}
                        <tr>
                            {# <td>{{ bricoleurs_demenageur.id }}</td> #}
                            <td>{{ bricoleurs_demenageur.Name }}</td>
                            <td>{{ bricoleurs_demenageur.LastName }}</td>
                            <td>{{ bricoleurs_demenageur.phone }}</td>
                            <td>{{ bricoleurs_demenageur.Job }}</td>
							<td class="text-center">
                                                <div class="list-icons">
													<div class="dropdown">
                                                            <a href="#" class="list-icons-item" data-toggle="dropdown"><i class="icon-stairs"></i></a>
                                                            <div class="dropdown-menu dropdown-menu-right">
                                                                <a href="{{ path('app_demenageurs_crud_show', {'id': bricoleurs_demenageur.id}) }}" class="dropdown-item"><i class="icon-eye"></i>Afficher</a>
                                                                <a href="{{ path('app_demenageurs_crud_edit', {'id': bricoleurs_demenageur.id}) }}" class="dropdown-item"><i class="icon-pencil5"></i>Modifier</a>
                                                                <a href="{{ path('app_demenageurs_crud_show', {'id': bricoleurs_demenageur.id}) }}" class="dropdown-item"><i class="icon-close2"></i>Supprimer</a>
                                                            </div>
													</div>
												</div>
							</td>
						</tr>
					{% else %}
						<tr>
							<td colspan="5">no records found</td>
						</tr>
					{% endfor %}
					</tbody>
				</table>
						</div>
						<!-- /Minuisiers list -->
			</div>
    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Last_name</th>
                <th>Phone</th>
                <th>Job</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bricoleurs_demenageur in bricoleurs_demenageurs %}
            <tr>
                <td>{{ bricoleurs_demenageur.id }}</td>
                <td>{{ bricoleurs_demenageur.Name }}</td>
                <td>{{ bricoleurs_demenageur.LastName }}</td>
                <td>{{ bricoleurs_demenageur.phone }}</td>
                <td>{{ bricoleurs_demenageur.Job }}</td>
                <td>
                    <a href="{{ path('app_demenageurs_crud_show', {'id': bricoleurs_demenageur.id}) }}">show</a>
                    <a href="{{ path('app_demenageurs_crud_edit', {'id': bricoleurs_demenageur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('app_demenageurs_crud_new') }}">Create new</a> #}
{% endblock %}
