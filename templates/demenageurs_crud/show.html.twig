{% extends 'base.html.twig' %}

{% block title %}Demenageurs{% endblock %}

{% block body %}
    <h1>Le Demenageur {{ bricoleurs_demenageur.Name }}</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ bricoleurs_demenageur.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ bricoleurs_demenageur.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_demenageur.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_demenageur.phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_demenageur.Job }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_demenageurs_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_demenageurs_crud_edit', {'id': bricoleurs_demenageur.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>

    {{ include('demenageurs_crud/_delete_form.html.twig') }}
{% endblock %}
