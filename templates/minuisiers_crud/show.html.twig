{% extends 'base.html.twig' %}

{% block title %}Minuisiers{% endblock %}

{% block body %}
    <h1>Le Minuisier {{bricoleurs_minuisier.Name}}</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ bricoleurs_minuisier.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ bricoleurs_minuisier.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_minuisier.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_minuisier.Phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_minuisier.Job }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_minuisiers_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_minuisiers_crud_edit', {'id': bricoleurs_minuisier.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>
    {{ include('minuisiers_crud/_delete_form.html.twig') }}
{% endblock %}
