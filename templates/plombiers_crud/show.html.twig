{% extends 'base.html.twig' %}

{% block title %}Show Plombier{% endblock %}

{% block body %}
    <h1>Le Plombier {{bricoleurs_plombier.Name}}</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ bricoleurs_plombier.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ bricoleurs_plombier.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_plombier.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_plombier.Phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_plombier.Job }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_plombiers_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_plombiers_crud_edit', {'id': bricoleurs_plombier.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>

    {{ include('plombiers_crud/_delete_form.html.twig') }}
{% endblock %}
