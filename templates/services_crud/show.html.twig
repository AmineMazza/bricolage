{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
    <h1>Le Service {{ service.NameService }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ service.Image }}</td>
            </tr>
            <tr>
                <th>Name_Service</th>
                <td>{{ service.NameService }}</td>
            </tr>
            <tr>
                <th>Discription</th>
                <td>{{ service.discription }}</td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('app_services_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:20px">Retour à la liste</a>
        <a href="{{ path('app_services_crud_edit', {'id': service.id}) }}" class="btn btn-primary" style="width:150px; margin-top:-38px; margin-left:200px">Modifier</a>
        <div style="margin-left:260px;margin-top:-48px">
            {{ include('services_crud/_delete_form.html.twig') }}
        </div>

{% endblock %}
