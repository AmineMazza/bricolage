{% extends 'base.html.twig' %}

{% block title %}Jardiniers{% endblock %}

{% block body %}
    <h1>Le Jardinier {{ bricoleurs_jardinier.Name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bricoleurs_jardinier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bricoleurs_jardinier.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_jardinier.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_jardinier.Phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_jardinier.Job }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_jardiniers_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_jardiniers_crud_edit', {'id': bricoleurs_jardinier.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>
    {{ include('jardiniers_crud/_delete_form.html.twig') }}
{% endblock %}
