{% extends 'base.html.twig' %}

{% block title %}Le Platrier {% endblock %}

{% block body %}
    <h1>Le Platrier {{ bricoleurs_platrier.Name }} </h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ bricoleurs_platrier.id }}</td>
            </tr>
            <tr> #}
                <th>Name</th>
                <td>{{ bricoleurs_platrier.Name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ bricoleurs_platrier.LastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ bricoleurs_platrier.Phone }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ bricoleurs_platrier.Job }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_platriers_crud_index') }}" class="btn btn-danger" style="width:150px; margin-top:10px; margin-left:80px">back to list</a>
    <a href="{{ path('app_platriers_crud_edit', {'id': bricoleurs_platrier.id}) }}" class="btn btn-primary" style="width:150px; margin-top:10px; margin-left:80px">edit</a>
    {{ include('platriers_crud/_delete_form.html.twig') }}
{% endblock %}
